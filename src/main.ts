import "./main.scss";

// Write a function that determines whether a button has been pressed
// All the buttons can go under a singular element and can use a forEach loop to determine whether they have been pressed
// Change the DOM of the Output screen to display the number that has been placed
// store a variable that converts the string into a number that returns the sum
